import 'package:flutter/material.dart';

class UpdateBoard extends StatelessWidget {
  const UpdateBoard({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
